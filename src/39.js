function generateRandomNodejsCode() {
  // Your JavaScript code goes here
  return `
    const Math = require('math-problems');
    const result = Math.random();
    console.log(result);
  `;
}

generateRandomNodejsCode();
